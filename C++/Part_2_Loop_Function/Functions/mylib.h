#ifndef MYLIB_H
// if not define, this line is to prevent import the prototype twice,
// which will cause compilation error

// define
#define MYLIB_H

// write the prototype, in mylib.h the header file
bool isEven(int number);

#endif