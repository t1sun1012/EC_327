//Provided code
/**
 * This function checks to see if you will get a job
 This function should return true if:
 1. The avatar is an Engineer and they get a A, B, or C in ece327
 2. The avatar is is Poet and they have a rich uncle
 Otherwise it should return false
 
 If it returns false, nothing should happen to bank account
 If it returns true, add 100,000 to change bank account

 * @param avatar - avatar value (1 = Engineer, 2=Poet) //Can assume only one of these numbers
 * @param richUncle - true for a rich uncle, false otherwise
 * @param grade - a single character for grade //Can assume A-D and UPPERCASE
 * @param bankAccount - amount in a bankAccount
 */

 //Add the getAJob prototype here; don't worry about include guards
 // ?? getAJob(??);

bool getAJob(int avatar, bool richUncle, char grade, double& bankAccount);

