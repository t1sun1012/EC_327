#include <iostream>
#include <fstream>
using namespace std;


//Fix this function
?? addGPA(??)
{
	
	//set the element in the list at the index provided to gpa
	//??[i] = gpa;
	return;	
}

//Fix this function
?? determineGPA(??)
{
	double gpaTotal = 0;
	for (int j=0; j<i; ??)//figure out the loop action-after-each-iteration
	{
		cout << "Course #" << j+1 << " = " << list[j] << endl;
		//increment the total by the current element in the list
		//gpaTotal += ??;
	}
	//determin the total divided by how may elements thre are
	//ave = ??;
	return;
}

//Fix this function
?? addNewClass(??)
{
	//create the array with a size big enough for the old elements plus the new one
	//double *returnList = ??;
	for(int i=0; ??; i++)//determine the loop continuation condition
		//returnList[i] = ??;//copy the orginal elements into the new element 
	//returnList[size] = ??;//add the new gpa
	
	//?? add a line here that is going to modify a variable to take into account the new size of the array 
	
	return returnList;
}

//Fix this function
?? readExternalInfo(??)
{
	ifstream input;	
	input.open(inputFile);
	int sum = 0;
	int temp;
	//Pick the right while statement
	/*
	while(input.eof())
	while(!eof())
	while(!input.eof())
	while(!input)
	*/
	{
		input >> temp;
		//add temp to a growing sum value
		//??;
	}
	//Pick the right statement
	/*
	input.close();
	input.exit();
	close.input();
	*/
	return sum;
}











